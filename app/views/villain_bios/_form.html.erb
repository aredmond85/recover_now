<div class="container">
   <div class="row justify-content-center">
      <div class="col-10">
         <%= render 'shared/error', model: @villainbio %>
         <%= form_with(model: @villainbio, class: "shadow p-3 mb-3 bg-dark rounded", local: true) do |f| %>
         <div class="form-group row">
            <%= f.label :villain_summary, class: "col-2 col-form-label text-light" %>
            <div class="col-10">
               <%= f.text_area :villain_summary, rows: 10, class: "form-control shadow rounded", placeholder: "Villain Summary" %>
            </div>
         </div>
         <div class="form-group row">
            <%= f.label :villain_powers, class: "col-2 col-form-label text-light" %>
            <div class="col-10">
               <%= f.text_area :villain_powers, rows: 10, class: "form-control shadow rounded", placeholder: "Please put a comma between each power" %>
            </div>
         </div>
         <div class="form-group row">
            <%= f.label :villain, class: "col-2 col-form-label text-light" %>
            <div class="col-10">
               <%= f.collection_select(:villain_id, Villain.all, :id, :villain_name, {prompt: "Select a Villain"}, {:required => true}) %>
            </div>
         </div>
         <div class="btn-toolbar p-2 mb-2 row justify-content-center">
            <%= f.submit class: "btn btn-primary" %>
         </div>
         <% end %>
      </div>
      <div class="mb-3">
         <%= link_to '[ Cancel and return to reports listing ]', reports_path, class: "text-info" %>
      </div>
   </div>
</div>