<div class="container">
   <h1 class="row justify-content-center">Villain Index</h1>
   <table class="table table-bordered bg-light">
      <thead bgcolor="#959595">
         <tr>
            <th scope="col">Villain Name</th>
            <th scope="col">Edit</th>
            <th scope="col">Delete</th>
         </tr>
      </thead>
      <tbody>
         <% @villains.each do |villain|%>
         <tr>
            <td><%= villain.villain_name %></td>
            <td><%= link_to "Edit villain", edit_villain_path(villain) %></td>
            <td><%= link_to "Delete villain", villain_path(villain), method: :delete, data: {confirm: "Are you sure?"} %></td>
         </tr>
         <% end %>
      </tbody>
   </table>
   <%= link_to 'New Villain', new_villain_path, class: "btn btn-primary" %>
</div>